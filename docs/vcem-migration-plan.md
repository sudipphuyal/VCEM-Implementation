# VCEM Migration Plan

| Existing module or contract | Current purpose | Required VCEM enhancement | Disposition | Backward compatibility impact | Required tests | Priority |
| --- | --- | --- | --- | --- | --- | --- |
| `contracts/Registries.sol` | Address-based role registry. | Add pseudonymous ID registry, active/revoked lifecycle, scoped roles, role events, and active-wallet lookup. | Retain legacy; add `VCEMRegistry`. | No breaking change to legacy tests. | role assignment, revocation, wallet remap, paused behavior. | High |
| `contracts/DSA/DataSharingAgreement.sol` | Patient-to-professional sharing agreement. | Preserve immutable consent versions instead of deleting; replace plaintext fields with hashes in VCEM path. | Retain legacy; supersede for VCEM with `VCEMConsent`. | Existing DSA API remains unchanged. | create/update/revoke, historical lookup, hash chain. | High |
| `contracts/RSA/ResourcesSharingAgreement.sol` | Resource sharing and access event prototype. | Bind each access to active consent hash in same transaction, verify signatures, replay protection, no raw resource/email data. | Retain legacy; add `VCEMAudit`. | Existing RSA API remains unchanged. | authorized/denied access, replay, expiry, outdated hash, actor/purpose/scope checks. | High |
| `contracts/ResourceCertification.sol` | Raw resource certification by trusted issuers. | Store data/resource hashes and pseudonymous subject IDs only. | Retain legacy; do not use for VCEM data release. | Existing certification tests remain unchanged. | hash-only fixture coverage in proxy tests. | Medium |
| `contracts/Utils.sol` | Legacy ID and duration helpers. | Avoid for VCEM consent hashing; use `sha256(abi.encode(...))`. | Retain. | None. | existing tests. | Medium |
| `contracts/DataSharingAgreementZKP.sol` | Simple proof-gated DSA. | Redesign ZKP extension to prove active commitment, actor inclusion, purpose/scope authorization, nullifier uniqueness. | Retain as experimental legacy; add docs and optional extension scaffolding. | Existing ZKP tests remain unchanged. | current verifier tests plus future nullifier tests. | Medium |
| `contracts/ABVerifier.sol` | Generated Groth16 verifier. | Link verifier output to VCEM active consent commitment before using in audit flow. | Retain as generated experimental artifact. | None. | verifier acceptance/rejection. | Low |
| `contracts/DSA/DsaEnumerator.sol` | Legacy DSA listing. | Fix pagination if used for new code; not needed for VCEM audit reconstruction. | Retain; document bug. | None if untouched. | legacy pagination regression. | Low |
| `contracts/RSA/RsaEnumerator.sol` | Legacy RSA listing. | Fix pagination if used for new code; not needed for VCEM audit reconstruction. | Retain; document bug. | None if untouched. | legacy pagination regression. | Low |
| `ignition/modules/*` | Contract deployment modules. | Add VCEM deployment module and manifest generation script. | Extend. | No breaking change. | deploy script smoke test. | High |
| `circuits/*` | Prototype Poseidon commitment circuits. | Add VCEM-oriented circuit design or mark current circuit limits. | Retain; add `zkp/` docs/scaffold. | None. | circuit compile/proof tests when dependencies exist. | Medium |
| `test/*.ts` | Legacy unit tests. | Add VCEM unit, integration, matrix, and verifier tests. | Extend. | None. | full VCEM required test list. | High |
| `.env` | Local secrets/config. | Remove from Git tracking, add `.env.example`, rotate exposed values. | Remove from index. | Developers must create local `.env`. | secret scanning/documentation check. | High |
| `build/`, `*.ptau`, `*.zkey`, proof JSON | Generated ZKP artifacts. | Classify reproducibility artifacts; avoid committing sensitive/local outputs. | Retain only intentionally documented artifacts. | May affect proof tests if removed later. | proof verification with documented artifacts. | Medium |
| No service layer | None. | Add API/data-proxy/audit-verifier/FHIR/pseudonymization modules. | Add. | New functionality only. | proxy bypass denial, receipt verification, fixture mapping. | High |
| No Besu infra | None. | Add 4-validator + RPC IBFT 2.0 Docker scaffolding and manifest scripts. | Add. | New functionality only. | config validation. | Medium |
| No benchmark harness | None. | Add reproducible workload scripts without fabricated results. | Add. | New functionality only. | script lint/smoke where possible. | Medium |
