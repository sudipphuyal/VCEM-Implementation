# Remediation Plan

| Gap | Affected Existing Files | Required Change | New Files if Needed | Tests | Evidence Artifact | Priority |
| --- | --- | --- | --- | --- | --- | --- |
| Caller-supplied `actorsRoot` is not bound to enforced actors. | `contracts/vcem/VCEMConsent.sol`, `VCEMTypes.sol`, `test/VCEM.ts` | Sort bounded actor IDs, reject zero/duplicates, derive root on-chain, store ordered actors, expose getters. | `contracts/vcem/libraries/CanonicalHash.sol` | Actor-root determinism, duplicate rejection, actor add/remove historical checks. | VCEM test output and matrix JSON/CSV. | Critical |
| Consent hash omits status and ZKP commitment. | `VCEMConsent.sol` | Use canonical SHA-256 over previous hash, participant, version, status, purpose mask, scope, actors root, ZKP commitment, timestamp. | `CanonicalHash.sol` | Hash recomputation and history checks. | Audit-verifier report. | Critical |
| Ambiguous lifecycle `UPDATED`. | `VCEMTypes.sol`, docs, tests | Rename semantics to `SUPERSEDED`; modification creates new `ACTIVE`, old version becomes `SUPERSEDED`. | `docs/consent-lifecycle.md` | Update/revoke lifecycle tests. | VCEM tests. | High |
| Purpose is index-like, not canonical bit. | `VCEMConsent.sol`, `VCEMAudit.sol`, FHIR mapper, tests | Define `TREAT=1`, `RESEARCH=2`, `PUBHLTH=4`, `OTHER=8`; reject malformed purpose values. | `docs/policy-model.md`, shared TS constants if needed. | Valid and malformed purpose tests. | VCEM tests. | High |
| Access events omit consent version/actors root. | `VCEMAudit.sol`, `VCEMTypes.sol`, `scripts/auditVerify.ts` | Persist and emit consent version and actors root. | None | Event assertion tests. | Audit logs. | High |
| Denied events are reason-hash only. | `VCEMAudit.sol`, tests | Add explicit reason enum and structured denied event. | None | Reason-code tests. | Denied-event logs. | Medium |
| Audit verifier is limited. | `scripts/auditVerify.ts` | Reconstruct consent chain, recompute hashes and actor roots, verify access events and bytecode. | `services/audit-verifier/` | Unit/integration tamper tests. | JSON/CSV audit report. | High |
| Data proxy bearer-token risk. | `services/data-proxy/*` | Add authenticated one-time delivery grants bound to caller, chain, event, request ID, and finality. | `services/api/`, `services/auth/`, `services/storage/` | Replay, forged receipt, mismatch, erasure tests. | Proxy test report. | High |
| FHIR mapper is fixture-only. | `services/fhir-adapter/*`, docs | Add explicit canonical policy conversion, fixtures, unsupported construct rejection. | `fixtures/fhir/`, `docs/fhir-limitations.md` | Fixture mapping tests. | FHIR test output. | Medium |
| Besu scaffold is not runnable. | `infrastructure/besu/*`, `package.json` | Generate validator keys/genesis/static peers; add up/down/status/verify scripts. | `infrastructure/besu/generated/` ignored. | Besu smoke/health checks. | Deployment manifest. | Medium |
| Default suite fails legacy ZKP tests. | `test/ABVerifier.test.ts`, `test/DataSharingAgreementZKP.test.ts`, ZKP artifacts | Regenerate aligned proof/verifier or isolate as explicit legacy experimental suite with docs. | possibly regenerated artifacts | Default and legacy test commands. | CI logs. | High |
| Benchmark methodology incomplete. | `benchmarks/*` | Add seed/run/analyze scripts, real API workload, PostgreSQL baseline semantics. | `benchmarks/reports/` | Script smoke tests. | Raw benchmark artifacts when run. | Medium |
| CI absent. | `.github/workflows/*` | Run install, lint, compile, tests, VCEM matrix, FHIR tests, audit verifier tests, static analysis where available. | GitHub Actions workflow. | CI. | CI artifact summary. | Medium |
